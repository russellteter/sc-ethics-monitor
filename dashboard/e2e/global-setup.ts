// Global setup hook for Playwright. Reserved for future use:
// fixture mutation, pre-test seeding, auth bootstrap, etc.
async function globalSetup() {
  return undefined;
}

export default globalSetup;
